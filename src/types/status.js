/**
 * @typedef {('LOW' | 'MEDIUM' | 'HIGH')} Importance
 * @typedef {('PENDING' | 'COMPLETED')} Status
 * @typedef {('TASK' | 'BIRTHDAY')} ItemType
 * @typedef {('FAR' | 'IMMINENT' | 'DUE' | 'OVERDUE')} TimeStatus
 */

// Esporta i tipi per JSDoc
export {};
